name: common-docker
lang: unknown
lang_version: unknown
git:
  enable: true
semaphore:
  enable: true
  execution_time_limit: {"hours": 1}
  pipeline_type: cp-dockerfile
  docker_repos: ['confluentinc/cp-base-new', 'confluentinc/cp-jmxterm']
  os_types: ["ubi8"]
  nano_version: true
  maven_skip_deploy: false
  pip_install_package: 'tox==3.28.0'
  push_latest: true
  use_packages: true
  cp_images: true
  tasks:
    - name: cp-dockerfile-build
      branch: master
      pipeline_file: .semaphore/cp_dockerfile_build.yml
      parameters:
        - name: CONFLUENT_VERSION
          required: true
          default_value: 'NONE'
        - name: PACKAGES_URL
          required: true
          default_value: 'NONE'
        - name: PACKAGES_MAVEN_URL
          required: true
          default_value: 'NONE'
        - name: PACKAGING_BUILD_NUMBER
          required: true
          default_value: 'NONE'
        - name: ALLOW_UNSIGNED
          required: true
          default_value: 'False'
          options:
            - 'True'
            - 'False'
        - name: CONFLUENT_DEB_VERSION
          required: true
          default_value: '1'
    - name: cp-dockerfile-promote
      branch: master
      pipeline_file: .semaphore/cp_dockerfile_promote.yml
      parameters:
        - name: CONFLUENT_VERSION
          required: true
          default_value: 'NONE'
        - name: IMAGE_REVISION
          required: true
          default_value: '1'
        - name: UPDATE_LATEST_TAG
          required: true
          default_value: 'NONE'
        - name: PACKAGING_BUILD_NUMBER
          required: true
          default_value: 'NONE'
        - name: PROMOTE_OS_TYPE
          required: true
          default_value: 'NONE'
          options:
            - 'deb'
            - 'ubi'