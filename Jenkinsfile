#!/usr/bin/env groovy

common {
    slackChannel = '#kafka-warn'
    downStreamRepos = ["rest-utils", "ksql", "newwave",
        "kafka-connect-replicator", "hub-client"]
    nanoVersion = true
    timeoutHours = 3
}
